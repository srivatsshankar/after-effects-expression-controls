// A script to toggle the usage of an expression using a checkbox
box = effect("Checkbox Control 1")("Checkbox"); // remember to provide the name for your checkbox
if (box == 1) {
	// provide an expression
} else {
	value;
}