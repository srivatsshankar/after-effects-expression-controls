// A looping function
loopOut(type = "cycle")
// the type can range from cycle, pingpong, continue, and offset
loopIn() // this can be used for looping on entry as opposed to exit
loopOut(type = "cycle", numKeyframes = 2) // this specifies the number of keyframes