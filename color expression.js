// a script to control the expression of colours in after effects
// apply the script to the colour parameter of the layer that requires modification of colour
// the expression control can be moved to the "essential graphics" tab
thisComp.layer("Controller").effect("Color Control")("Color")
// replace "controller" with your layer
